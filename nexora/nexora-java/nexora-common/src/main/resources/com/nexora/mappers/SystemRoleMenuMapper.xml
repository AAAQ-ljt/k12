<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexora.mappers.SystemRoleMenuMapper">

	<!--实体映射-->
	<resultMap id="base_result_map" type="com.nexora.entity.po.SystemRoleMenu">
		<!--主键-->
		<id column="id" property="id"  />
		<!--角色：0管理员（学生端无菜单权限，不进此表）-->
		<result column="role_type" property="roleType"  />
		<!--菜单ID-->
		<result column="menu_id" property="menuId"  />
		<!--创建时间-->
		<result column="create_time" property="createTime"  />
	</resultMap>


	<!-- 通用查询结果列-->
	<sql id="base_column_list">
		 s.id,s.role_type,s.menu_id,s.create_time
	</sql>

	<sql id="base_condition_filed">
			<if test="query.id != null">
				 and  s.id = #{query.id}
			</if>
			<if test="query.roleType != null">
				 and  s.role_type = #{query.roleType}
			</if>
			<if test="query.menuId != null">
				 and  s.menu_id = #{query.menuId}
			</if>
			<if test="query.createTime != null and query.createTime!=''">
				 <![CDATA[ and  s.create_time=str_to_date(#{query.createTime}, '%Y-%m-%d') ]]>
			</if>
	</sql>

	<!-- 通用查询条件列-->
	<sql id="query_condition">
	 <where>
			<include refid="base_condition_filed" />
			<if test="query.createTimeStart!= null and query.createTimeStart!=''">
				 <![CDATA[ and  s.create_time>=str_to_date(#{query.createTimeStart}, '%Y-%m-%d') ]]>
			</if>
			<if test="query.createTimeEnd!= null and query.createTimeEnd!=''">
				 <![CDATA[ and  s.create_time< date_sub(str_to_date(#{query.createTimeEnd},'%Y-%m-%d'),interval -1 day) ]]>
			</if>
	 </where>
	</sql>

	<!-- 查询集合-->
	<select id="selectList" resultMap="base_result_map" >
		 SELECT <include refid="base_column_list" /> FROM system_role_menu s <include refid="query_condition" />
		 <if test="query.orderBy!=null">
			 order by ${query.orderBy}
		 </if>
		 <if test="query.simplePage!=null">
			 limit #{query.simplePage.start},#{query.simplePage.end}
		 </if>
	</select>

	<!-- 查询数量-->
	<select id="selectCount" resultType="java.lang.Integer" >
		 SELECT count(1) FROM system_role_menu s <include refid="query_condition" />
	</select>

	<!-- 插入 （匹配有值的字段）-->
	<insert id="insert" parameterType="com.nexora.entity.po.SystemRoleMenu">
		<selectKey keyProperty="bean.id" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
		 INSERT INTO system_role_menu
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="bean.roleType != null">
				 role_type,
			</if>
			<if test="bean.menuId != null">
				 menu_id,
			</if>
			<if test="bean.createTime != null">
				 create_time,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="bean.roleType!=null">
				 #{bean.roleType},
			</if>
			<if test="bean.menuId!=null">
				 #{bean.menuId},
			</if>
			<if test="bean.createTime!=null">
				 #{bean.createTime},
			</if>
		 </trim>
	</insert>

	<!-- 插入或者更新 （匹配有值的字段）-->
	<insert id="insertOrUpdate" parameterType="com.nexora.entity.po.SystemRoleMenu">
		 INSERT INTO system_role_menu
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.id != null">
				 id,
			</if>
			<if test="bean.roleType != null">
				 role_type,
			</if>
			<if test="bean.menuId != null">
				 menu_id,
			</if>
			<if test="bean.createTime != null">
				 create_time,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.id!=null">
				 #{bean.id},
			</if>
			<if test="bean.roleType!=null">
				 #{bean.roleType},
			</if>
			<if test="bean.menuId!=null">
				 #{bean.menuId},
			</if>
			<if test="bean.createTime!=null">
				 #{bean.createTime},
			</if>
		 </trim>
		 on DUPLICATE key update 
		 <trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.roleType!=null">
				 role_type = VALUES(role_type),
			</if>
			<if test="bean.menuId!=null">
				 menu_id = VALUES(menu_id),
			</if>
			<if test="bean.createTime!=null">
				 create_time = VALUES(create_time),
			</if>
		 </trim>
	</insert>

	<!-- 添加 （批量插入）-->
	<insert id="insertBatch" parameterType="com.nexora.entity.po.SystemRoleMenu" useGeneratedKeys="true" keyProperty="id">
		 INSERT INTO system_role_menu(
			 role_type,
			 menu_id,
			 create_time
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.roleType},
				 #{item.menuId},
				 #{item.createTime}
			 )
		 </foreach>
	</insert>

	<!-- 批量新增修改 （批量插入）-->
	<insert id="insertOrUpdateBatch" parameterType="com.nexora.entity.po.SystemRoleMenu">
		 INSERT INTO system_role_menu(
			 role_type,
			 menu_id,
			 create_time
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.roleType},
				 #{item.menuId},
				 #{item.createTime}
			 )
		 </foreach>
			on DUPLICATE key update 
			role_type = VALUES(role_type),
			menu_id = VALUES(menu_id),
			create_time = VALUES(create_time)
	</insert>

	<!--多条件修改-->
	<update id="updateByParam" parameterType="com.nexora.entity.query.SystemRoleMenuQuery">
		 UPDATE system_role_menu s
 		 <set> 
			<if test="bean.roleType != null">
				 role_type = #{bean.roleType},
			</if>
			<if test="bean.menuId != null">
				 menu_id = #{bean.menuId},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
 		 </set>
 		 <include refid="query_condition" />
	</update>

	<!--多条件删除-->
	<delete id="deleteByParam">
		 delete s from system_role_menu s
 		 <include refid="query_condition" />
	</delete>

	<!-- 根据Id修改-->
	<update id="updateById" parameterType="com.nexora.entity.po.SystemRoleMenu">
		 UPDATE system_role_menu
 		 <set> 
			<if test="bean.roleType != null">
				 role_type = #{bean.roleType},
			</if>
			<if test="bean.menuId != null">
				 menu_id = #{bean.menuId},
			</if>
			<if test="bean.createTime != null">
				 create_time = #{bean.createTime},
			</if>
 		 </set>
 		 where id=#{id}
	</update>

	<!-- 根据Id删除-->
	<delete id="deleteById">
		delete from system_role_menu where id=#{id}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectById" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from system_role_menu s where id=#{id}
	</select>

</mapper>