# 习题管理

习题管理

```
阅读项目，解析来开发教学业务相关功能。
首先开发习题管理，相关的表在database.sql里面。后端基础代码已经生成，继续完成相关前后端代码，保证功能正常，交互好用。
新增，修改题目，选项列表做成可以拖拽排序，拖拽排序后，先定义AB.C，D这样的字母的数组，然后根据选项索引，展示对应的字母即可，同时选项编号重新编排，清空正确答案选项。
选择的选项校验为必填，使用el的动态表单校验
答案选项，答案解析都是必填
```

试卷管理
```
接下来开发试卷管理相关功能，试卷分为课后习题，以及考试试卷.
试卷后端的基础代码已经生成。编辑试卷采用抽屈弹窗组件，左侧是试卷编排，右侧是预览，预览效果跟真实的考试试卷类似。抽屈弹窗要占到屏幕的80%。
左侧是试卷编排，添加分组后，分组后面增加加入题目按钮，点加入题目按钮，弹窗选择题库中的问题，题目选择列表增加复选框进行选择，已选择的题目要展示选项。选择题展示选项，预览也展示选项。判断题也展示选项。
右侧的预览时题目编号，分数，题型，标题在同一行。试卷预览分组自动加上编号，使用一，二这样的大题编号。

获取选项使用批量查询的方案，选择题目后，根据题目ID批量查询选项结果，不要循环查询。
```


课程管理前端

```
完成课程管理相关的前端功能，采用mock数据，先不写后端.
包含创建课程，章节管理，课时管理，视频关联课时，课件关联课时，课后习题关联课时。

列表中编辑，查看，删除都是操作课程，不涉及章节信息，在列表中增加章节管理按钮。

章节管理优化
1、分为两栏，左侧是章节列表，可以新增章节，新增章节不采用弹窗，直接插入即可
2、右侧分为上下两部分，上面一部分是编辑章节信息，包含章节名称，章节说明，还可以删除章节，下面一部分是具体的课时信息。
3、课时信息可以新增课时，新增时直接插入，不要弹窗，卡片列表显示，主要包含课时名称，选择视频作为主视频，也可以删除视频，重新选择。第二列显示显示课件，课件从资源中心选择，选择的课件卡显示，最多可以选择五个课件资源，第三列是作业，同样卡片显示，选择试卷库中的试卷。主视频必须选择，课件和课后习题非必选。

课程管理 主要包含  课程名称，授课老师不用选择，
可以展示，就展示当前登录的人，课程封面，从资源中新选择，将资源中心封装为一个可以弹窗选择的组件，
后续的资源选择都可以使用。选择授课班级，课程状态，其他内容不需要了，先修改这个，
具体的课程章节信息，先不变。


优化以下几点
1、新增章节按钮放到章节列表标题后面，不用输入章节名称，默认给一个未命名章节的名称。新增课时同理，默认给一个未命名,添加后，课时信息是可编辑状态
2、右侧章节展示的章节信息，一直保持可编辑状态
4、课件资源选择，可以选择任何类型的资源。
5、课后习题只能选择一个


1、调整章节列表的增按钮，将其放到课时列表的右侧，使用link形式的按钮。添加一个保存章节按钮（每一章节都需要保存，所有数据，点击保存后才存储。），将删除章节的按钮和保存章节的按钮放在一起，不用分的太开。
2、课时列表里的课时也需要支持拖拽
```

课程管理后端
```
审查代码，继续完成教学业务里的课程管理相关的功能。
课程管理包含了章节管理。
1、去掉课程管理mock的数据，采用真实的后端数据，完成前后端的功能，数据库文件在database.sql。
2、先只做课程管理相关的，章节管理相关功能先不用处理，保证课程管理相关功能完整，没有问题，主要是增删改查。后端的基础代码，已经生成，在原有基础上继续开发，参照已经开发完成的模块。
3、在新增课程里面，里面的授课教师应该根据当前登录用户来获取当前用户的real_name，比如当前登录用户user_no为xxxxx，可以根据xxxxx去查询real_name，或者将real_name跟user_no一样一起存储到本地并携带到token。


接下来完善章节管理相关功能。
注意事项：
1.新增课程的时候，数据库没有正确插入授课班级的信息，database.sql里面有三张course，请查看审阅，保证授课信息能够正确插入。
2.保存章节，最后保存的时候，将所有数据组合到一起提交到后台，后台解析相关的数据，写入到对应的表中。目前保存章节，课时，相关的功能都正确。


审查项目，完善教学业务里的课程管理相关的功能。
重点查看数据库文件database.sql里面的course相关的5张表，理清相互调用的关系，后端在调用的时候要保证规范，例如事务及其他数据库优化操作。
当前项目bug：
1.在新增课程后，选择授课班级后，会发现数据库已经存储了信息，但是前端页面不显示。然后重点是首次编辑课程的时候（第二次修改就正常了），它会创建几条新的数据，同时course也进行了修改，正常来说，应该是获取course_id后，修改class_id。此时就变成了修改course_id，重新写入几条course_info相关的数据，请修复。
2.章节管理在点击一次后不能再次点击，需要刷新才能再次点击
3.点开章节管理之后，新增章节后，在该章节对应的信息下保存课时，会发现数据根本没有入库，前端显示完好，后端也无报错。




```
