# Nexora 开发流程与 AI 交接文档

> 最后更新：2026-08-21
> 状态：本文件是后续开发唯一执行依据；历史排期文档已删除，不再维护排期承诺。

## 维护规则

每个功能块完成后，按以下顺序维护本文件：

1. 更新 `功能归档表`：新增一行，记录功能块、完成日期、关键提交、核心结果。
2. 更新 `下一阶段任务队列`：把刚完成的功能标为已完成，把下一块标为“开发中”。
3. 更新 `第 7 节`对应小节标题状态为“已完成”（只改状态标注，不删除设计正文）。
4. 更新本文件并提交 git。

文档三层职责：第 5 节是“已完成归档表”，第 6 节是“下一步任务队列”，第 7 节是“功能块设计档案”（已完成 / 进行中 / 待开发 / 已取消的设计都保留在这里）。第 7 节不是待办清单，只有第 6 节是；功能完成后在第 5 节归档、第 6 节推进，第 7 节仅更新标题状态。

开发方式：完成一块功能 → 构思并确认下一块功能 → 再开发；验收以真实前后端联调为准。

## 1. 项目定位

Nexora 是 K12 人工智能通识课教学助手（赛题 JBGS-2026-02），产品名含义为 Nexus 连接 + Aurora 极光。

- 管理端：维护学生、用户、资源、知识库、提示词等内容。
- 学生端：AI 助教、绘本、课程教材、个性化学习路径、编程环境等学习场景。
- 后端：Spring Boot 3 多模块，管理端 / 学生端 / MCP 服务拆分。
- 核心角色：仅学生 + 管理员两类；AI 智能体承担“7×24 在线教师”职责。
- 学段：小学低 / 小学高 / 初中 / 高中，UI 已按“年级”表达。

## 2. 工程结构

```text
nexora/
  nexora-java/
    nexora-common/   PO / Service / Mapper / 枚举 / DTO / VO
    nexora-admin/    管理端后端接口
    nexora-web/      学生端后端接口 + AI 对话 + Netty WebSocket
    nexora-mcp/      MCP 教学工具服务
  nexora-front/
    nexora-front-admin/  管理后台前端
    nexora-front-web/    学生端前端
  docs/              开发流程（唯一执行依据）
knowledge/          RAG 知识库源文件
```

## 3. 技术栈与统一约定

- 后端：Spring Boot 3.x、Java 21、MySQL 8、Redis、Elasticsearch、Spring AI、Netty WebSocket。
- 前端：React 19、TypeScript、Vite、antd v6、Zustand、React Router。
- 接口：`/api/<模块名>/<动作>`，统一返回 `ResponseVO<T>`。
- 约定：PO / DTO / VO 分离；AI 调用统一封装；AI Key 走环境变量；`参考项目/` 只读并已忽略。

## 4. 已确认的产品与技术方向

### 4.1 RAG 技术结论

- 向量存储使用 ES，metadata 固定为 `dataType / stage / knowledgePointId / difficulty`。
- 检索：向量优先，关键词回退；topK 10~15，相似度阈值 0.5。
- 无命中时明确告知“知识库中暂无相关内容”，禁止编造。
- 不引入 MySQL BLOB 向量、自研 HNSW、Google ADK 全套、多供应商网关。

### 4.2 个人 AI 知识库

- 方案 A 已确认：服务端个人私有空间，数据仅对本人可见。
- 回答顺序：个人知识库 → 官方知识库 → 通用对话。
- 检索必须按 `ownerId` 隔离。
- Wiki 借鉴结论见 4.4：raw 原始资料不可变 + wiki 知识页由 AI 维护，图谱只做派生视图。

### 4.3 提示规范

- 所有解析/操作提示统一使用 antd `message`：成功、警告、失败分别用 `success / warning / error`。
- 提示不使用 Modal 弹框；资源选择器等选择场景可以使用弹窗。

### 4.4 优秀开源参考：llm-wiki-skill-main（2026-08-15）

参考项目位于 `参考项目/llm-wiki-skill-main`（Karpathy LLM Wiki 方法论的完整实现，含 Skill 形态 + agent 工作台），只读，不引入代码，只借鉴思路。

#### 4.4.1 核心模型

- 三层结构：`raw/` 原始素材（不可变事实来源）、`wiki/` 知识层（AI 维护、可复利）、schema 配置（规则与工作流）。
- 核心动作：`init / ingest / query / digest / lint / status / graph / delete / crystallize`。
- 页面类型：素材摘要、实体页、主题页、对比分析、综合分析、保存的查询结果；统一 frontmatter + `[[wikilink]]` 互链。
- 辅助资产：`index.md`（目录）、`log.md`（操作日志）、`purpose.md`（研究目标与关键问题）、`.wiki-cache.json`（SHA256 去重）、`.wiki-schema.md`（维护规则 + 别名词表）。

#### 4.4.2 可借鉴机制

- 消化分两步：先结构化分析（entities / topics / connections / confidence / evidence），再生成页面；格式校验失败自动回退单步，避免脏数据。
- 置信度四档：`EXTRACTED / INFERRED / AMBIGUOUS / UNVERIFIED`，AI 生成内容必须可回溯到原文或明确标注推断。
- SHA256 缓存：同一素材无变化直接复用已有结果；删除素材时级联清理引用、缓存与断链。
- 查询链路：先读索引 + 别名词表展开，再检索页面与命中段落，控制上下文上限，回答必须附引用；有价值的回答可沉淀为新页面。
- 批量消化：便宜模型做 digest 子代理，并发受限，子代理只输出 Markdown，主进程写盘；主对话仍用主模型。
- Lint：扫描孤立页、断链、矛盾、索引缺失和置信度报告，只给建议，确认后才修改。
- 图谱：是 wiki 页面关系的派生视图，不是事实来源；想改结构就改页面，再重新生成图谱。
- 工程取舍：本地优先，知识库 / 应用状态 / 模型凭证三类数据严格分离；不用 MCP（本地过重）；SSE 流式；Obsidian 兼容（纯 Markdown，AI 不碰第三方元数据）；会话绑定知识库防止跨库污染。

#### 4.4.3 Nexora 落地建议

- 不照搬本地文件系统与 Obsidian，映射到现有 MySQL + ES + 资源中心：
  - raw = `resource_info`（`owner_id` 非空，即学生个人资源）；
  - wiki 页面 = 新增 `wiki_page`（`owner_id / pageType / title / contentMd / frontmatter / sourceIds / confidence / status`），或先以 Markdown 文本存 `knowledge_doc`；
  - index/log = 页面统计 + 操作记录；purpose = 学生「我的学习目标 / 关键问题」配置；
  - 去重 = `knowledge_doc.sourceResourceId` + 文件 SHA256，重复导入跳过。
- 个人库检索顺序维持：个人知识库 → 官方知识库 → 课程资源；wiki 页结构化结果优先于原文 chunk，回答附页面引用。
- 消化任务复用现有 Redis 异步队列：提取文本 → 分块 → 向量化 + AI 生成摘要/实体/主题页 → 更新索引与日志；失败可重试。
- 删除资源时级联删除个人 wiki 页与 ES chunk；图谱/知识地图放到后期，只做轻量派生视图，不做独立工作台。
- 不引入：本地 Obsidian 依赖、MCP 服务、多模型角色、独立图谱引擎；当前单模型 + 现有异步队列足够。

### 4.5 题目公式与图片方向

- 数学公式使用 Markdown + LaTeX：行内 `$...$`、块级 `$$...$$`，后台编辑器与学生端统一用 KaTeX 渲染；数据库保存 LaTeX 原文。
- 含图题的图片统一引用资源中心 `resource_id`，写入 `question_info.question_image`（逗号分隔，支持一题多图）。
- AI 出题只生成不含图片的题目；含图题由管理员手动补图或导入题目 MD 时携带 `![图1](@resourceId)` 解决（PDF 自动解析已放弃，见 7.18）。

### 4.6 参展核心策略（2026-08-21 收敛定稿）

- 核心链路：跑通「资源上传 → 向量化 → AI 助教问答」。官方库 = 提取 → AI 整理结构化 MD → 管理员确认 → 向量化；个人库 = 上传 → AI 生成 Wiki → 用户确认 → 向量化。两侧共用同一套「AI 结构化整理」能力。
- 多模态：选 5 种（对话问答、多模态教学、动画讲解、绘本生成、编程环境），放弃「游戏化/趣味」层；保留最小「AI 出题 → 客观题判分 → 掌握度/练习记录」闭环待团队最终确认，为个性化学习路径与学习分析供数。
- 数学公式：保留 LaTeX 存储 + KaTeX 渲染；放弃 PDF 公式自动解析，数学计算/推导交给大模型能力。
- 理科意图分流：做题/计算/公式推导/实验等理科求解类直接走模型；概念与知识点查询仍走 RAG 优先。覆盖数学、物理、化学、生物。
- 图片/视频向量化：仅以「标题 + 简介」作为向量化内容。
- 模型策略：当前 DeepSeek 仅用于开发测试；最终目标为多模态、教育能力强的模型（如 Gemini），后续切换时只改配置，AI 调用保持统一封装。
- 角色：仅学生 + 管理员，无教师 / 班级 / 固定考试；后台资料由管理员统一维护，后续按需扩展权限。

## 5. 功能归档表（已完成）

| 功能块 | 完成日期 | 关键提交 | 核心结果 |
|---|---|---|---|
| P0 地基校准 | 2026-08-12 | `c3bdf56`、`8445491`、`4ebab38` | AGENTS 对齐、编译修复、管理端登录联调、Redis 登录态简化 |
| P1 账号体系闭环 | 2026-08-13 | `adaf2f1`、`d02b733`、`bf9c2f7` | 管理端用户管理、学生端登录注册、路由守卫、学段转年级 |
| AI 最小对话闭环 | 2026-08-13 | `ac65b8c` | DeepSeek 流式对话、会话/消息/取消、绘本独立页 |
| P4-M1 意图路由 | 2026-08-13 | `dffe601` | 意图枚举、Prompt 模板、意图分析组件 |
| 资源管理 | 2026-08-14 | `60cfc98`、`e2772ee`、`a4b9396`、`e227234`、`1cec6e8` | 目录树、分片上传、视频转码、预览、批量删除 |
| M2 官方 RAG | 2026-08-14 | `0cbd4c4`、`5932506` | 知识总览/知识目录/问答测试、ES 入库、学生端检索、目录中文展示 |
| BaseDrawer 组件封装 | 2026-08-14 | `1afa174` | 受控抽屉，与 BaseDialog 风格一致，关闭自动重置表单，用于资源解析入库 |
| 资源解析入库 | 2026-08-14 | 本次提交 | txt/md/docx/ppt/pdf 解析、Redis 异步队列、向量化分批 15 条、超链接文档、知识目录按钮重构 |
| AI 助教资料推荐 + 课程教材学习页 | 2026-08-14 | 本次提交 | RAG 命中透出推荐卡片（resourceId/resourceType/title），学生端课程教材列表与视频/图片/文档/链接预览，新增学生端资源读取接口 |
| 教学业务-课程管理 | 2026-08-14 | 本次提交 | course_info/question_info 增加 grade；管理端课程 CRUD、章节/课时/资源绑定；学生端按年级展示课程列表与课程详情 |
| 管理端菜单-教学业务 | 2026-08-15 | `7e4eb8b` | 一级菜单合并为「教学业务」，四个子页：课程管理/习题管理/试卷管理/考试管理；习题保留题目列表与 AI 出题审核 Tabs |
| MCP 启动修复 | 2026-08-15 | 本次提交 | 学生端后端 MCP 客户端默认关闭（`NEXORA_MCP_CLIENT_ENABLED` 控制），连接地址补齐 `/mcp`，不再因 MCP 服务未启动而阻断 Web 后端 |
| 教学业务-习题管理基础 | 2026-08-15 | `d6a5548` | 题目 CRUD、选项维护、年级筛选、审核上架；新增题目报错修复，知识点必选并校验学段一致性 |
| 开发流程体系 | 2026-08-14 | `6c39704`、`716123d`、`22045db`、`5acfe0f` | 开发流程唯一依据、功能块设计、Maven 路径记录 |
| 优秀开源参考：llm-wiki-skill-main | 2026-08-15 | 本次提交 | 个人知识库方向落地结论：raw/wiki/schema 三层、ingest/query/lint 工作流、页面模板、置信度与去重机制，详见 4.4 |
| 用户个人学习情况（学习分析子页） | 2026-08-18 | `a1340539` | 管理端学习分析新增用户列表与学习详情抽屉：课程进度、练习统计、个人知识库、AI 对话与 Token、知识点掌握度；设计见 7.13 |
| 学习分析-个人知识库收尾（检索测试 + 原始资源阅览 + 向量化内容阅览） | 2026-08-18 | 本次提交 | 用户个人学习情况个人知识库 Tab 新增知识库检索测试、学生原始资源预览/下载、向量化文档列表与内容预览；学生文档上传自动解析向量化并支持个人库优先检索；图片/视频向量化与 AI wiki 页列入后续 TODO |
| 运行修复与参展设计定稿 | 2026-08-21 | `dabdaef`、`fdde7b7` | MySQL 连接修复（allowPublicKeyRetrieval）、新增禁止 Codex 编译约束；收敛版设计定稿（5 种多模态、个人 Wiki 两段式、AI 文档整理、理科意图分流），设计见 4.6 / 7.14-7.18 |
| P0 个人资源中心三层化 + 个人 Wiki 两段式（代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 默认目录 raw/wiki/attachments（`resource_directory.dir_type`）、上传按类型自动归类、raw 仅 md/txt、上传不再自动向量化；「生成 Wiki → 草稿可编辑 → 确认向量化」链路落地：`StudentWikiGenerateComponent` 分段 AI 整理，草稿 `vectorStatus=0`，确认置 1 入队消费后置 2；关键词检索与学习分析只统计已确认页；删除资源级联清理知识页与向量；`user_wiki_profile` 学习档案表 + 读改接口 + 前端表单；迁移脚本 `nexora-java/sql/20260822_student_wiki.sql`；设计见 7.12 / 7.16；由用户执行 SQL 并编译重启后联调验收 |
| P1 理科意图分流 + 图片/视频轻量向量化（代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 新增 `SCIENCE_SOLVE` 意图与理科求解提示词；意图分析「关键词规则（数学/物理/化学/生物词表+解题句式，概念类前缀豁免）+ LLM」双保险，命中直接模型解答不检索；生成类意图同步跳过 RAG 检索（省 embedding/ES 调用）；学生上传图片/视频自动建「标题+简介」轻量知识页草稿（dataType=IMAGE/VIDEO），确认时刷新最新简介后入库，检索命中跳资源预览；前端资源编辑弹窗新增简介输入；官方库图片/视频向量化挂主线 2；设计见 7.14 / 7.15，待用户编译重启后联调验收 |
| 动画讲解产物链路（对话内生成 SVG 分步讲解，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | ANIMATION 意图不再只回文本：`AnimationScriptComponent` 生成分步 SVG 讲解脚本 JSON（LLM + 逐帧白名单清洗，非法 SVG 帧丢弃、超长截断），产物落 `ai_generation_record` 并以 `bizType=ANIMATION` 卡片经 WS 推送；前端新增 `SvgStepPlayer` 分步播放器（步骤导航 + SVG 画面 + 讲解文字，渲染前二次剥离危险结构），AI 助教对话内卡片播放 + 动画独立页列表/全屏播放/删除；生成失败自动降级为文字讲解；设计见 7.20，待用户编译重启后联调验收 |
| 对话内出题卡片（L4，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | QUIZ 意图不再只回文本：`QuizGenerateComponent` 生成选择测验 JSON（3-5 道单选、选项/答案/解析，学段适配），`bizType=QUIZ` 卡片经 WS 推送；前端 `QuizCard` 答题卡片即时判分（对/错标记 + 解析 + 得分/重练）；历史消息重放保留卡片；生成失败自动降级为文字出题；暂不写 practice_record / knowledge_mastery（等「最小答题判分闭环」最终确认，见主线 10）；设计见 7.21，待用户编译重启后联调验收 |
| 管理端 AI 文档整理页（主线 2，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | AI 结构化整理能力下沉 common（`AiStructureComponent`，学生 wiki 与管理端共用，见 7.17.3）；官方资源「提取文本 → AI 整理结构化 MD → 管理员编辑确认 → 带知识点向量化入库」链路：`POST /api/knowledgeBase/aiOrganize` + 管理端「AI 文档整理」页（左资源选择/右工作区，原始文本对照 + 整理稿编辑 + 确认入库表单），确认入库复用 resourceImport（content 模式）；设计见 7.17，待用户编译重启后联调验收 |
| 绘本生成一期（主线 8，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 低年级绘本：`PictureBookGenerateComponent` 故事分页文案（LLM JSON）+ 逐页文生图（dashscope qwen-image-3.0，同步+异步任务轮询，失败降级纯文字页）+ 图片下载本地；产物存 `resource_info`（PICTURE_BOOK + ext_json 分页）；接口 `/api/pictureBook/{generate,myList,getInfo,del,image}`；前端 `PictureBookReader` 翻页阅读器 + 绘本页（主题生成入口 + 列表 + 阅读弹窗）；页图媒体接口公开直连（与现有 image 一致）；设计见 7.22，待用户编译重启后联调验收 |
| 编程环境一期（主线 7，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 学生端编程环境占位页升级：Monaco（@monaco-editor/react，CDN loader）+ Pyodide 浏览器内运行 Python（CDN 发行版，首次加载约 10-30s）；按学段预置代码框架（小高循环/游戏、初中列表/turtle、高中排序/递归）；stdout/stderr 采集 + 运行耗时 + 清空/重置；「保存学习」将代码以 .md 存入个人库 raw（可继续生成知识页）；设计见 7.23，前端构建后即可联调（无后端改动） |
| 答疑后动作卡片 + 对话同步知识页（L2/L3，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | AI 讲解回复下方动作卡片（同步知识页 / 出题练习 / 动画讲解）：同步知识页走 `POST /api/studentWiki/syncFromMessage`（取该条问答生成 wiki 草稿，source_url=agent-message:{messageId} 去重、同消息重复同步覆盖，草稿态 vectorStatus=0 待确认）；出题/动画动作复用现有意图链路继续对话；设计见 7.24，前端构建后即可联调 |
| 习题批量导入页重构（主线 5，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 废弃 PDF 自动解析（页图/插入页图入口移除）：习题管理 Tab 更名「批量导入」；左侧 MD 编写（`@uiw/react-md-editor`）+ 右侧 KaTeX 实时预览（remark-math/rehype-katex）；「解析 Word 为 MD 初稿」（`POST /api/questionImport/parseDocx`，POI 段落+表格提取）；下载 MD / 解析并入库（复用 parseQuestions/batchAddQuestions，草稿待审核）；设计见 7.18，待用户编译重启后联调验收 |
| 课程教材 → 个人 wiki 同步（主线 6，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 课程详情页「生成知识页草稿」按钮 → `POST /api/courseInfo/syncWiki`：AI 将课程绑定资源（章节→课时→资源，名称+简介，上限 30 条）整理为结构化知识页草稿（`source_url="course:"+courseId` 按课程去重、重复同步覆盖），待用户确认入库；设计见 7.16/7.17（AiStructureComponent 共用），待用户编译重启后联调验收 |
| 项目报告初稿（主线 11） | 2026-08-22 | 本次提交（待提交） | `docs/项目报告.md`：架构 / 大模型策略 / 多模态路线（6 种）/ 知识库两段式 / 学段自适应 / 个性化基础 / 难题与方案 / 部署与集成 / 评分对照 / 已知限制;随功能增量同步 |
| 个性化学习路径页（AI 生成版，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 学生端学习路径占位页升级：`LearningPathGenerateComponent` 结合学习档案（目标/兴趣/关键问题/术语）+ 已确认知识页生成分阶段计划 JSON（学/练/复习 4-8 步）；存 `ai_generation_record`（type=LEARNING_PATH）；接口 `/api/learningPath/{generate,myList,del}`；前端路径卡片列表 + 生成/删除；管理端「学习路径管理」页（LearningPath 视图）暂未联动（learning_path 主表后续按知识点节点化时接入） |
| AI 学习报告（主线 9 最小主体，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | 管理端学习分析 → 用户详情抽屉新增「AI 生成学习报告」：`GET /api/learningAnalysis/aiReport`（复用六类统计聚合 userDetail → 组装文本摘要 → LLM 生成 Markdown 报告：总体评价/亮点/薄弱环节/针对性建议/下一步行动，只使用真实数据不编造）；前端 Modal + `MDEditor.Markdown` 渲染；学习行为统一落 student_learning_record 与周报/掌握度 AI 渲染视图待后续 |
| MCP 教学工具服务（7.19，代码交付，待联调验收） | 2026-08-22 | 本次提交（待提交） | `nexora-mcp` 空壳补齐：`TeachingToolService` 白名单 5 组 6 工具（queryKnowledgePoint / queryCourse / queryLesson / recommendResource / queryMastery / saveLearningRecord，均返回结构化字符串且 try-catch 不抛未捕获异常；saveLearningRecord 校验学生存在 + 行为类型白名单 + 目标对象存在）；`ServerRegisterConfig` 用 `MethodToolCallbackProvider` 注册；`student_learning_record` 补 common 全套（PO/Query/Mapper/XML/Service/Impl，改造既有表无 DDL）；删除空 `InitRun.java`；待用户编译启动 8084 后 `tools/list` 验证 |
| 联调验收手册 | 2026-08-22 | 本次提交（待提交） | `docs/联调验收.md`：前置条件 / 15 分钟评审演示路线 / 14 项交付逐项验收清单（含预期结果）/ 常见问题排查表 / 反馈格式 |
| 多模态视觉对话 + 动画产物入库（代码交付，待联调） | 2026-08-22 | 本次提交（待提交） | AI 助教支持图片上传/粘贴（图存个人库 → `imageResourceIds` 随消息发送 → 视觉模型 `deepseek-v4-flash-vision-exp`，`project.ai.vision-model` 可配，带图不传 reasoning-effort）；用户消息 `bizType=USER_IMAGE` 历史重放缩略图；动画产物改存 `resource_info`（ANIMATION 类型、附件目录、ext_json=脚本），对话内只给小卡片跳 `/animation`，全屏播放页 `/animation/:resourceId`；绘本二期 TTS 配音取消（一期即完整形态） |
| 账号邮箱化 + 资源存储分离 + 团队一键迁移（代码交付） | 2026-08-22 | 本次提交（待提交） | 账号体系以邮箱为唯一登录标识（`user_info` 删 `uk_username`、保留 `uk_email` 与内部主键 `user_id`）；资源文件按 `files/admin/{月}` 与 `files/student/{邮箱目录}/{月}` 分目录（绘本图 `student/{邮箱}/picture-book/`）；`nexora-base`：从开发者库导出的**纯结构 SQL**（35 表、业务数据清除）+ `migrate.bat`（清除旧库重建并导入最新结构），脚本随目录迁移至 `docs/sql/` |
| 绘本异步任务编排（主线 8 二期，代码交付） | 2026-08-22 | 本次提交（待提交） | 长链路彻底异步化：`POST /pictureBook/generate` 秒回 taskId → Redis List 队列 + `PictureBookTaskConsumer` 状态机（PENDING→STORY_GENERATING→STORY_DONE→IMAGE_GENERATING→COMPLETED/FAILED，进度/页图实时持久化，TTL 2h）→ 故事→分页→**3 路并发生图**逐张回写 → 落 `resource_info`（附件目录）；前端 2s 轮询进度（编写故事/绘制插图 x/N 页），**任务状态存 sessionStorage，切页不丢、切回自动恢复进度或打开成品**；单页失败降级纯文字 + 全失败原因写入 `imageError` 展示 |
| 豆包(火山方舟 ARK)文生图接入 + 绘本版权主题净化（代码交付） | 2026-08-22 | 本次提交（待提交） | `project.ai.image.provider=ark` 默认（`NEXORA_IMAGE_PROVIDER` 可切回 dashscope）：ARK 走 OpenAI 兼容 `/images/generations`（`doubao-seedream-5-0-pro-260628`、`output_format=png`、`size=1.5K`、`optimize fast`、`NEXORA_ARK_API_KEY`，单次 90s 超时 + 429/网络异常重试 3 次，错误归类 Key/限流/额度/内容安全）；**版权预设**：`sanitizeTopic` 主题净化（米老鼠→爱冒险的小老鼠等，30 字内原创表述，失败回落原主题）+ 故事提示词强制原创角色 + 生图提示词禁 IP 元素；绘本开放**整个小学阶段**（小低+小高） |
| 小学阶段禁用动画 + 模型验证页（代码交付） | 2026-08-22 | 本次提交（待提交） | 动画讲解入口对小学阶段（PRIMARY_LOW/PRIMARY_HIGH）全隐藏（欢迎建议词/动作按钮/模式切换）且后端意图硬拦截（`ANIMATION`→`CHAT` 文字讲解，防误用绘本生图链路）；管理端「系统设置 → 模型验证」页三路连通性测试（DeepSeek 对话 / 百炼向量 / 豆包文生图，`/api/modelTest/{chat,embedding,image}`，错误含 Key 掩码与原因归类）；绘本/助教等慢接口超时与 WS 切页体验修复（输入框宽度、登录验证码焦点、预览 ObjectURL 等） |

## 6. 下一阶段任务队列（2026-08-21 八条主线版）

> 组织方式：本表以 8 条产品主线为核心，另附学习分析、试卷作答、报告、MCP 等兜底/配套项。优先级 P0 → P4；M3/M4/M5 不再作为硬性里程碑。第 6 节只展示待推进与进行中项，已完成项在 5 归档、设计细节在 7 保留。

| 主线 | 状态 | 优先级 | 依赖 | 关键子项 / 验收口径 |
|---|---|---|---|---|
| 1. 用户端个人资源中心（raw / wiki / Attachments） | 代码交付 → 待联调验收 | P0 | 现有资源中心（7.10 / 7.12） | 默认目录 raw/wiki/attachments 已完成（`resource_directory.dir_type` 系统目录，存量账号幂等补齐）；raw 仅 md/txt 且未选目录时自动归类已完成；上传不再自动向量化、两段式「生成 Wiki → 草稿可编辑 → 确认向量化」已完成（草稿 `vectorStatus=0`，检索/学习分析只收已确认页，删除资源级联清理）；`user_wiki_profile` 学习档案表 + 学习档案表单已完成。**待联调验收**：执行 `nexora-java/sql/20260822_student_wiki.sql` 后编译重启，按 7.16.4 验收口径走一遍；附件粘贴图片 + Obsidian `![[...]]` 引用渲染与 word/pdf 白名单扩展留待下一迭代 |
| 2. 管理端 AI 文档整理页 | 代码交付 → 待联调验收 | P2 | 官方 RAG、ResourceKnowledgeParser、AI 封装 | 独立页面已完成：「AI 文档整理」菜单（/knowledge/arrange）左选资源 → AI 提取整理结构化 MD（可编辑）→ 原始文本对照 → 确认入库（学段/知识点/难度，复用 resourceImport 的 content 模式异步向量化）；超长文档分段整理已完成（AiStructureComponent MAX 6000 字/段）；与学生个人 Wiki 共用同一「AI 结构化整理」组件（AiStructureComponent，7.17.3）；待联调验收：选资源 → AI 整理 → 编辑 → 确认入库 → 学生端问答命中且回复质量提升；官方图片/视频轻量向量化（dataType=IMAGE/VIDEO）挂本主线下一迭代 |
| 3. 理科意图分流 + 图片/视频轻量向量化（个人 Wiki 两段式已随主线 1 落地） | 代码交付 → 待联调验收 | P1 | 意图路由、7.14 / 7.15 | 理科解题/计算/推导/实验直达模型已完成（`SCIENCE_SOLVE` 意图 + 关键词规则双保险，概念查询仍走 RAG，生成类意图同步跳过检索）；图片/视频以标题+简介建 knowledge_doc 已完成（学生端个人库，自动草稿 + 确认刷新 + 已确认过滤）；待联调验收：问「帮我解一元二次方程」等直出步骤不依赖检索；上传图片/视频 → 知识页确认 → AI 助教能检索到并跳预览；官方库图片/视频向量化挂主线 2 |
| 4. AI 调用线路打通（L1-L6） | 部分完成 → 推进中 | P1 | 3、意图路由、动画/出题能力 | L1 上传 → AI 助教页解析 → 资源中心审查修改 → 同步向量库；**L2 答疑后动作卡片已完成**（同步知识页 / 出题练习 / 动画讲解，见 7.24）；**L3 对话同步知识页已完成**（`/studentWiki/syncFromMessage` 消息级去重草稿）；**L4 对话内出题卡片已完成，见 7.21**；**L6 动画产物链路已完成，见 7.20**；L5 后台 RAG 卡片 + 外部资源白名单推荐待开发 |
| 5. 管理端习题 PDF 导入页重构 | 代码交付 → 待联调验收 | P2 | 习题 MD 规范 | 已重构（Tab 更名「批量导入」）：左侧 MD 编写 + 右侧 KaTeX 预览 + docx 解析按钮（POI 段落/表格提取填入初稿）；放弃 PDF 自动解析与页图模式（旧 PDF 后端保留不推荐）；AI 出题保留；下载 MD / 直接解析入库（草稿待审核）；待联调验收：docx 上传 → 初稿 → 规范整理 → 入库 |
| 6. 课程教材 → 个人 wiki 同步 | 代码交付 → 待联调验收 | P2 | 1、4-L3 | 已完成：课程详情页「生成知识页草稿」→ `POST /courseInfo/syncWiki`（AI 读取课程绑定资源名称+简介整理为知识页草稿，`source_url="course:{courseId}"` 按课程去重，上限 30 条资源）；确认后入知识层（两段式既有链路）；待联调验收：课程页按钮 → 知识页草稿出现 → 确认入库后 AI 可检索 |
| 7. 编程环境 + AI 链路 | 一期代码交付 → 待联调验收 | P3 | 前端 Pyodide | 一期已交付：Monaco + Pyodide 浏览器运行 Python、学段预置框架（小高/初中/高中）、运行输出与耗时、「保存学习」落个人库 raw（.md，可继续生成知识页）；AI 链路的「保存本次学习生成 wiki 草稿」待 L3 联动（当前经 raw 资源间接可达）；turtle 画布渲染与代码调试器（断点）留二期 |
| 8. 低年级绘本生成 | 一期代码交付 → 待联调验收 | P3 | 多模态图像 / 音频模型 | 一期已交付：主题 → 故事大纲（4-8 页文案）→ 逐页文生图（dashscope qwen-image-3.0，同步+异步轮询，失败降级纯文字）→ 产物存 `resource_info.ext_json`（PICTURE_BOOK）→ 翻页阅读（`PictureBookReader`）+ 绘本页生成入口/列表/删除；**二期（TTS/音乐配音、故事朗读）待开发**；联调注意：需 `NEXORA_DASHSCOPE_API_KEY` 有效且图像模型可用，插图生成约每页 5-20 秒 |
| 9. 学习分析（知识层 + AI 渲染） | 部分完成 → 推进中 | P1 | 1、4-L3、student_learning_record | 已交付：学生端个性化学习路径页（AI 结合档案 + 已学知识生成计划）+ 管理端「AI 生成学习报告」（六类统计 → LLM Markdown 报告，`GET /learningAnalysis/aiReport`）；剩余：学习行为统一落 student_learning_record、周报/掌握度 AI 渲染视图 |
| 10. 试卷管理-学生端作答（管理端已完成主体） | 待确认 | P2 | 4-L4 / 出题能力 | 练习卷抽题作答与判分；客观题判分与 practice_record / knowledge_mastery 写入取决于「最小答题判分闭环」最终确认 |
| 11. 项目报告 + 部署说明 | 初稿完成 → 随功能增量同步 | P4 | 各主线收尾 | `docs/项目报告.md` 初稿已写：架构 / 大模型策略 / 多模态路线 / 知识库构建 / 学段自适应 / 个性化基础 / 难题与方案 / 部署与集成 / 评分对照；随功能增量同步更新 |
| 12. M3 MCP 教学工具 | 代码交付 → 待联调验收 | P4 | 核心链路完成后 | 白名单 5 组 6 工具已完成（知识点/课程/课时/资源推荐/掌握度/学习记录写入，`student_learning_record` common 全套补齐，`ServerRegisterConfig` 注册）；待用户启动 8084 后 `tools/list` 验证；web 端 MCP 客户端维持默认关闭（`NEXORA_MCP_CLIENT_ENABLED=false`）；设计见 7.19 |

建议推进顺序：

- P0：主线 1（个人资源中心三层改造，所有 AI 线路的地基）。
- P1：主线 3、4（理科分流 / 个人 Wiki 两段式 / 图片视频向量化 + L1-L6 链路）与主线 9（学习分析提前与知识层对齐，按知识层由 AI 渲染生成）。
- P2：主线 2（AI 文档整理页）、5（习题 PDF 导入页）、6（课程 → wiki）、10（试卷作答）。
- P3：主线 7（编程环境）、8（绘本生成一期）。
- P4：绘本二期音频、MCP、项目报告 / 部署说明。

## 7. 已完成：资源解析入库

### 7.1 目标

打通资源管理与知识库：管理端选择已上传资源，解析抽取文本 → 清洗 → 分块 → 向量化入库，学生端 AI 可直接检索。

前置组件：`BaseDrawer`（已完成，提交 `1afa174`）用于资源选择、解析详情等抽屉场景。

### 7.2 文件类型与处理方式

| 类型 | 处理方式 | 注意点 |
|---|---|---|
| `.txt` / `.md` | 直接读取文本 → 分块 → 向量化 | 无解析风险 |
| `.docx` | Apache POI `XWPF` 提取段落与表格 | 支持较好 |
| `.doc` | Apache POI `HWPF` 尽力提取 | 老格式可能丢内容，用 `message.warning` 提示转成 `.docx` |
| `.ppt` / `.pptx` | Apache POI `XSLF` 遍历每页 Slide 提取文本框 | 图片/图表内文字无法提取；备注可一并提取 |
| `.pdf` | PDFBox `PDFTextStripper` 提取文本 | 扫描型 PDF 提取不到文字 |
| 视频 / PPT / 超链接 | 用户主动填写资料 | 复用知识目录“新增文档”方式 |

### 7.3 PDF 扫描版判断

- 按页统计有效字符数，文本为空或极少时标记为“疑似扫描版”。
- 使用 `message.warning` 提示需要 OCR 处理，不阻断其他操作。
- 第一版不接入 OCR；后续再接 PaddleOCR 或独立 OCR 服务。

### 7.4 入库流程与幂等

```text
选择资源 → 落库（处理中）→ Redis 异步队列 → 检测类型 → 提取文本 / 用户手动填写
    → 清洗 → 分块（复用 TextChunker） → 分批向量化写入 ES（每批 ≤15 条）
    → 更新 knowledge_doc（sourceResourceId / vectorStatus / chunkCount）
```

- 同一资源重复导入时，通过 `sourceResourceId` 更新已有文档，不产生重复。
- 重新解析先删除旧 ES chunk，再写入新 chunk。
- 大文件已异步解析：接口先返回任务状态，后台队列处理解析与向量化；`vectorStatus` 表示处理中/成功/失败。
- `sourceType` 扩展为：0 手动维护 / 1 资料解析 / 2 手动填写资源说明。
- 超链接文档走「文档录入」的 `sourceUrl` 字段，`dataType=LINK`，检索结果携带 `sourceUrl` 供学生端跳转。

## 7.5 已完成：教学业务-课程管理

### 7.5.1 数据模型

- `course_info` 新增 `grade`（年级），保留 `stage` 冗余；`grade -> stage` 统一走 `StageEnum.matchByGrade`。
- `question_info` 同步新增 `grade`，供后续习题按年级抽题。
- 迁移脚本：`nexora-java/sql/20260814_course_grade.sql`（不改写既有 DDL）。

### 7.5.2 管理端

- 课程 CRUD + 详情树：`/courseInfo/*`。
- 章节 CRUD：`/courseChapter/*`；删除章节级联删除课时、资源绑定与学习进度。
- 课时 CRUD：`/courseChapterLesson/*`；删除课时级联删除资源绑定与学习进度。
- 课时资源绑定：`/courseChapterLessonResource/*`，资源复用 `resource_info`，不复制文件。
- 前端课程管理页支持年级筛选与新增/编辑年级；「章节管理」抽屉内维护章节、课时与课时资源。

### 7.5.3 学生端

- `/courseInfo/loadMyCourses`：按 Redis 登录态中的 `grade` 精确过滤（无年级时回退 `stage`）。
- `/courseInfo/getDetail`：返回课程、章节、课时与可用资源，仅上架且年级匹配的课程可见。
- 课程教材页升级为课程列表 → 课程详情（章节/课时/资源）→ 资源预览（`/course-material/resource/:resourceId`）。

## 7.6 已完成：教学业务-习题管理（基础版）

### 7.6.1 数据模型（已有）

- `question_info`：题型 `0单选 1多选 2判断 3填空 4简答 5解答 6论述 7材料分析`；`grade/stage` 冗余；难度 `1-3`；来源 `0管理员录入 1AI生成`；审核 `0待审核 1已上架 2已驳回`；`score` 默认分值。
- `question_option`：选择题选项，维护 `optionLabel / optionContent / isAnswer / sort`。
- `20260814_course_grade.sql` 已为 `question_info` 增加 `grade` 与索引。

### 7.6.2 管理端

- 题目列表：按年级、题型、难度、审核状态、题干关键词检索。
- 新增/编辑：单选/多选维护选项；判断/填空维护 `answer`；保存时按 `grade` 反推 `stage`。
- 审核：支持待审核/已上架/已驳回状态流转。
- 知识点必选：新增/编辑题目必须选择知识点，后端校验知识点存在且学段与年级一致；提交 `d6a5548` 修复了新增题目时 `knowledge_point_id` 为空导致的 SQL 报错。
- 自动录入的“草稿确认后再入库”原则保留，具体重构设计见 7.7。

### 7.6.3 学生端（后续）

- 按年级精确抽题，练习页展示题干、选项、答案与解析，并记录练习结果。

## 7.7 进行中：习题管理重构（AI 出题 + PDF 导入）

### 7.7.1 目标

题库量必须支撑 AI 助教，放弃“管理员一条条手动录入”作为主路径，改为两种批量生产方式，共用同一套「题目 MD 规范 + 解析器」，最终结构化写入 `question_info / question_option`。

### 7.7.2 题目 MD 规范（公共底座）

- 一道题一个 Markdown 块，包含：年级、难度、题型、分值、题干、知识点、选项、解析。
- 公式使用 LaTeX：行内 `$...$`，块级 `$$...$$`；数据库保存 LaTeX 原文，后台编辑器与学生端均用 KaTeX 渲染。
- 图片引用：`![图1](@resourceId)`，一题可多图；解析时写入 `question_info.question_image`（逗号分隔 `resource_id`）。
- 图片统一存放 `resource_info`，复用资源中心上传、预览、下载能力。
- 解析结果先出“结构化草稿”，管理员确认后才批量入库，AI 不直接写库。

### 7.7.3 AI 出题模式

- 页面：习题管理 Tabs 中“AI 出题审核”改为“AI 出题”。
- Drawer（BaseDrawer）占屏约 80%：左侧配置，右侧 MD 编辑器（`@uiw/react-md-editor` + KaTeX 预览）。
- 左侧配置项：
  - 年级、知识点（按年级对应学段联动）；
  - 出题描述；
  - 知识库资料（可选）：选择资料后以资料内容为主出题；
  - 题量分配：按简单 / 中等 / 困难三档，由管理员直接为每档分配各题型数量，不单独设置总题数。
- AI 只生成不含图片的题目；如果某题必须配图，不生成该题或由管理员后续手动补充。
- 右侧可微调题干、选项、答案、解析、公式。
- 产出可：下载 `.md`、保存到资源中心（作为 DOCUMENT 资源）、直接解析入库。

### 7.7.4 PDF 录入模式（已废弃，重构为 7.18）

- 放弃 PDF 文本/公式自动解析，不再做 PDFRenderer 页图 + 视觉模型转写。
- 改由管理员将 PDF 转 Word，经「docx 解析按钮」导入成 MD 草稿后入库；页面为左侧 MD 编写 + 右侧 KaTeX 预览，详见 7.18。

### 7.7.5 自动导入

- 题目列表页新增「自动导入」按钮。
- 两种入口：
  - 本地上传 MD 文件；
  - 从资源中心选择已上传的 MD / PDF 资源。
- 后端解析题目 MD → 返回结构化草稿 → 管理员确认 → 批量入库。
- 校验：字段完整性、知识点存在、题型 / 难度合法、图片 `resourceId` 存在且资源可用。

### 7.7.6 后端接口草案

- `POST /questionGenerate/generate`：AI 出题，返回题目 MD。
- `POST /questionGenerate/parseMd`：MD → 结构化题目草稿。
- `POST /questionInfo/batchAdd`：草稿确认后批量入库（题目 + 选项）。
- `POST /questionImport/parsePdf`：PDF → 草稿 MD + 页图列表。
- 页图 / 图片上传复用资源中心分片上传；MD 保存资源中心复用资源新增接口。

### 7.7.7 实施顺序（建议）

1. 题目 MD 规范 + 后端 `parseMd` + `batchAdd`；**已完成**
2. AI 出题生成接口 + 出题 Drawer；**已完成**
3. 习题 PDF 导入页重构（docx 解析 + 左侧编写右侧预览）；**待开发，见 7.18**
4. 自动导入按钮（上传 MD / 资源中心选择）；**已完成**
5. 学生端练习页：KaTeX 公式渲染、图片题渲染、按年级抽题。

## 7.8 进行中：试卷管理

- 数据模型：`paper_info` 试卷、`paper_group` 大题、`paper_question` 试卷题目（含分值），迁移脚本 `nexora-java/sql/20260815_paper.sql`。
- 管理端：试卷列表 + 新增/编辑/查看抽屉，左侧大题管理与题目检索，右侧实时预览；支持练习卷/考试卷、年级、状态、总分自动汇总。
- 接口：`/paperInfo/loadDataList`、`/paperInfo/getInfo`、`/paperInfo/save`、`/paperInfo/del`。
- 已完成：题目在试卷内上移/下移排序、本大题/整卷统一分值、Markdown 导出。
- 下一步：学生端试卷作答。

## 7.9 已取消：考试管理

- 结论：7x24 AI 教师不设固定时间考试，考试管理取消。
- 2026-08-15 已删除后端 `Exam*` 代码、`sql/20260815_exam.sql`、管理端 `views/exam/*` 与 `api/exam.ts`，菜单和路由已移除考试管理。
- 保留 `paper_info` 作为练习卷/测验卷，不再承载考试时间、考试状态。

## 7.10 进行中：学生端重构（初中/高中）

### 7.10.1 目标

学生端以“个人知识库”为核心，AI 助教成为“最懂你的 AI 教师”：学生上传资料、归档、解析，AI 基于个人知识库 + 官方知识库 + 课程资源回答问题、推荐内容、生成动画。

### 7.10.2 导航

初中/高中：

1. AI 助教
2. 动画讲解（AI 生成 HTML/SVG 动画，产物存资源中心，独立播放页）
3. 课程资料
4. 编程环境（后期）
5. 资源中心（个人知识库：树形目录、上传、解析、归档）
6. 学习路径（后期，由学习行为记录派生）
7. 我的

小学：AI 助教、绘本生成、课程资料、资源中心、我的；当前仅做导航占位。

### 7.10.3 DDL 设计

迁移脚本：`nexora-java/sql/20260815_student_learning.sql`（已于 2026-08-15 执行，`nexora` 库已落库）。

- `resource_info`：新增 `owner_id`（NULL=管理端公共资源，非空=学生个人资源）、`ext_json`（动画步骤/绘本分页等产物元数据）、索引 `idx_owner_status`。
- `resource_directory`：新增 `owner_id`，学生目录与学生资源按 owner 隔离。
- `knowledge_doc`：新增 `owner_id`（NULL=官方知识库，非空=学生个人知识库）。
- `student_learning_record`：新增学习行为记录表，记录 VIEW/COMPLETE/PRACTICE/ANIMATION/PARSE，作为学习路径与个性化数据基础。
- Elasticsearch 向量索引增加 `ownerId` metadata，检索时按 owner 过滤。

### 7.10.4 实施顺序

1. 学生端按学段导航改造；小学导航占位。**已完成**
2. 学生资源中心基础版：树形目录、同级拖拽排序、分片上传、Redis 异步队列、owner 隔离、图片/视频/文档预览。**已完成**
3. 个人知识库解析入库：文档解析 → 个人 `knowledge_doc` → 个人向量索引。**已完成（2026-08-18）**
4. AI 助教检索顺序：个人知识库 → 官方知识库 → 课程资源。**已完成（2026-08-18）**
5. 动画讲解独立页：AI 生成 HTML/SVG → 资源中心 → 播放器。**页面占位已建，播放器待开发**
6. 编程环境、学习路径后期实现。

## 7.11 已完成：管理端导航收敛

- 一级菜单收敛为：工作台 / 用户管理 / 资源中心 / 知识库管理（官方知识库）/ 教学业务 / 学习分析 / 系统设置。
- 教学业务子页：课程管理 / 习题管理 / 试卷管理；考试管理已删除。
- 学习分析展示真实统计：课程进度来自 `course_study_progress`、练习来自 `practice_record`、个人知识库活跃来自 `resource_info.owner_id`、AI 对话来自 `agent_message`。
- 系统设置子页：管理员账号 / 模型与提示词 / 环境配置。
- 新增后端 `SystemSettingController`、`LearningAnalysisController` + `LearningAnalysisMapper`，新增前端 `LearningAnalysis`、`AdminAccount`、`ModelPrompt`、`EnvConfig`。
- 管理端资源查询、预览、删除、转移均限定 `owner_id IS NULL`，与学生个人资源完全隔离。

## 7.12 进行中：学生个人知识库（文档向量化已完成；图片/视频向量化与 AI Wiki 见 7.15/7.16）

### 7.12.1 目标

学生端以个人知识库为核心：学生上传文档后自动解析、分块并向量化，AI 助教优先基于个人知识库回答，再回退官方知识库与课程资源；图片/视频保留在个人资源中心，后续再做向量化；AI 自动生成 wiki 页（素材摘要 / 实体 / 主题）留作后续。

### 7.12.2 初始化

- 账号创建 / 首次登录时自动初始化个人知识库：创建学生根目录与默认分类（文档 / 图片 / 其他），生成空的 `user_wiki_profile` 学习档案。
- 学生无需理解目录结构；知识库页提供「重新初始化 / 恢复」入口，不用知识库的学生可完全忽略。
- 不使用知识库不阻塞其它功能；初始化失败不阻断登录。

### 7.12.3 上传限制

- 知识库解析仅允许文档：`md / txt / docx / pdf / ppt / pptx`。
- 图片/视频单独维护在个人资源中心，仅保存、预览与下载，不进入文本解析与向量化；图片/视频向量化、引用到 wiki 页留作后续开发。
- 每人总上传额度 300MB：上传前校验文件大小与剩余额度，列表展示已用 / 剩余空间，超出时拒绝并提示。

### 7.12.4 学生可见配置

- 学生只看到「我的学习档案」：学习目标、关键问题、感兴趣学科 / 主题、自己的术语叫法（alias）。
- 完整 schema（页面规范、ingest / lint 规则、检索优先级）属于系统层，按学段 / 年级预设，不暴露给学生编辑。

### 7.12.5 存储方案

- 当前采用服务器私有存储：文件按 `owner_id` 隔离，仅本人可见，提供下载、导出、删除。
- 文件存储抽象为独立 storage 接口，后续可切换 MinIO / S3 / WebDAV / 自建对象存储，业务层不变。
- 纯本机目录存储需要桌面客户端（Tauri / Electron）并本地化 AI 与向量检索，属于另一产品形态，暂不实现。

### 7.12.6 实施顺序

1. 初始化个人知识库 + 上传类型白名单 + 300MB 额度。**已完成（2026-08-15）**
2. 个人文档消化任务：提取文本 → 分块向量化 → 更新索引与日志。**已完成（2026-08-18）**；两段式改造随主线 1 于 2026-08-22 完成：上传仅保存原始资源，AI 生成 Wiki 草稿，用户确认后才向量化（见 7.16.4；图片/视频轻量向量化见 7.15）。
3. AI 助教检索链路：个人文档向量化 chunk → 官方知识库 → 课程资源，回答附引用。**已完成（2026-08-18）**；两段式后个人库只检索已确认知识页（`vectorStatus=2`），草稿不可检索（2026-08-22）。
4. lint 健康检查与轻量知识地图放到后期。

### 7.12.7 当前完成度（2026-08-22）

- 已完成：学生文档上传后由用户「生成 Wiki」→ AI 结构化整理为草稿 → 用户编辑确认 → 向量化入库；个人库检索只查已确认页并优先于官方库；管理端学习分析可查看已确认向量化文档、检索测试，并预览/下载学生原始资源；学生学习档案（`user_wiki_profile`）读写。
- 后续：图片/视频向量化见 7.15；lint 健康检查与轻量知识地图放后期。

## 7.13 已完成：用户个人学习情况（学习分析子页）

### 7.13.1 目标

在管理端学习分析总览基础上，新增「用户个人学习情况」子页，按用户查看完整学习画像：课程学习进度、练习次数与正确率、个人知识库资源、AI 对话量与 Token 消耗、知识点掌握程度。

### 7.13.2 管理端页面

- 菜单：学习分析 → 用户个人学习情况（`/learning/user`，菜单编码 `learning:user`），页面风格沿用教学业务模块的 BaseTable / SearchForm / BaseDrawer。
- 列表接口：`GET /api/learningAnalysis/loadDataList`，支持用户名 / 昵称 / 邮箱模糊、学段、年级、状态筛选与分页。
- 列表字段：课程学习并完成数量与平均进度、练习总次数 / 正确数 / 正确率、知识库资源数与占用空间、AI 会话数 / 消息数 / Token 消耗、掌握度平均分与已掌握知识点数。
- 详情接口：`GET /api/learningAnalysis/getStudentDetail?userId=xxx`，返回学习概览、课程学习、练习情况、个人知识库、AI 对话、掌握度六类数据。
- 详情展示：BaseDrawer 宽 1100，六个 Tabs 分开展示；知识库 Tab 展示配额/类型分布/资源列表（学生原始资源预览与下载）、知识库检索测试、向量化文档列表与向量化内容预览；AI 对话 Tab 展示意图分布与最近消息。

### 7.13.3 后端实现

- `LearningAnalysisController` 新增用户列表与用户详情接口，查询与聚合 SQL 全部收敛在 `LearningAnalysisMapper.xml`，单用户详情一次性查出关联列表，避免循环查库。
- 新增 `LearningUserQuery`（继承 `UserInfoQuery`）与 `LearningUserSummaryVO / LearningUserDetailVO / CourseStudyProgressItemVO / PracticeKnowledgePointVO / PracticeQuestionTypeVO / KnowledgeResourceVO / AiIntentVO / AiRecentMessageVO / KnowledgeMasteryVO`。
- 统计口径：课程来自 `course_study_progress`，练习来自 `practice_record`，知识库来自 `resource_info`（`owner_id` 非空），AI 来自 `agent_session / agent_message`，掌握度来自 `knowledge_mastery`。
- 个人知识库收尾新增接口：`GET /api/learningAnalysis/studentDocList`（向量化文档分页）、`GET /api/learningAnalysis/studentDocDetail`（文档正文/分块详情）、`POST /api/learningAnalysis/searchTest`（按 `ownerId` 检索个人知识库）；`ResourceInfoController` 新增学生资源路由 `/studentVideo/*`、`/studentImage/*`、`/studentFile/*`、`/studentDownload/*`，均校验 `resource.ownerId == userId`。

### 7.13.4 验收口径

- 管理端可筛选用户并打开学习详情；六个 Tab 数据与后端统计口径一致；个人知识库 Tab 可预览/下载学生资源、执行知识库检索测试、查看向量化文档与内容；无数据场景显示 0 / 空状态，不报错；前端 `tsc -b && vite build` 通过。

## 7.14 已完成：理科意图分流（AI 助教）

### 7.14.1 目标

把「理科求解类」提问从知识库检索链路中分流出来，直接调用大模型能力，避免本地库无命中时答非所问；概念解释与知识点查询仍走 RAG 优先。

### 7.14.2 分流范围

- 走模型直达：做题、计算、公式推导、实验步骤、化学方程式配平、生物遗传计算等理科求解类。
- 仍走 RAG 优先：概念解释、知识点查询、基于教材/知识库内容的问题。
- 覆盖学科：数学、物理、化学、生物（理科族），不做单一「数学」限定。

### 7.14.3 实现

- 意图枚举扩展（对齐现有 `UserIntentEnum / PromptTypeEnum`），新增理科求解意图（如 `SCIENCE_SOLVE`）。
- `AgentChatComponent` 分流：`shouldSearch` / `resolvePromptWithRag` 按意图跳过检索并切换 System Prompt；用「关键词规则 + LLM 意图识别」双保险。
- 求解类强制不检索；概念类在命中高分时仍保留知识库上下文。

### 7.14.4 验收

学生提问「帮我解一元二次方程」「并联电阻怎么算」等命中求解类，直接输出步骤与答案，不依赖知识库命中。

### 7.14.5 实现记录（2026-08-22，代码交付待联调）

- `UserIntentEnum` 新增 `SCIENCE_SOLVE`；`PromptTypeEnum` 新增「理科求解提示词」（分步推导 + LaTeX + 学段适配）。
- `IntentAnalyzerComponent`：理科求解先走关键词规则（数学/物理/化学/生物词表 + 解题句式），命中直接返回 `SCIENCE_SOLVE`，不再调用模型（省一次调用）；未命中再走 LLM 分类（意图列表已含 `SCIENCE_SOLVE` 兜底）；「什么是/解释/为什么」等概念类前缀不进入关键词规则，避免误伤概念查询。
- `AgentChatComponent`：`shouldSearch` 对 `SCIENCE_SOLVE` 返回 false，且 RAG 检索整体按 `shouldSearch` 跳过（生成类意图 PICTURE_BOOK/ANIMATION/CODING 同步省掉多余的 ES 检索与 embedding 调用）；提示词按学段经 `PromptTemplateComponent` 解析（Redis → 表 → 枚举默认值）。
- 该意图无推荐卡片，`mapIntentToBizType` 返回 null；消息落库携带 `intent/bizType` 供学习分析统计。

## 7.15 已完成（学生端个人库）：图片/视频轻量向量化

### 7.15.1 目标

图片/视频以其「标题 + 简介/说明」作为向量化内容入库，让学生能检索到资源并跳转预览。

### 7.15.2 数据与链路

- 复用现有异步队列：为图片/视频建 `knowledge_doc`（content = 标题 + 简介、dataType = IMAGE/VIDEO、sourceResourceId 指向原资源）。
- 短文处理（通常 1 条 chunk），不生成正文大块向量。
- 检索命中后推荐卡片携带 `resourceId / resourceType`，跳资源预览。

### 7.15.3 范围

- 官方库与个人库统一支持；个人库记录纳入 7.16「已确认」过滤。

### 7.15.4 实现记录（2026-08-22，学生端个人库完成，官方库挂主线 2）

- 学生上传图片/视频成功后，`StudentResourceUploadServiceImpl` 自动建轻量 `knowledge_doc` 草稿（`dataType=IMAGE/VIDEO`、content=「标题：xxx\n简介：yyy」、`vectorStatus=0`），用户编辑资源简介后可在「知识页」列表确认入库（`vectorStatus=1` 入队 → 2 完成），确认时自动刷新为最新标题+简介（自动模板以「标题：」开头才刷新，用户手动编辑过的内容不覆盖）。
- 检索与推荐：复用现有 RAG（向量优先 + 关键词回退），命中后推荐卡片携带 `resourceId/resourceType` 跳转预览；个人库仍只检索已确认页。
- 前端：资源编辑弹窗增加「简介」输入项；知识页列表对 IMAGE/VIDEO 草稿同样支持编辑/确认/删除。
- 官方库图片/视频轻量向量化（管理端上传时建 `dataType=IMAGE/VIDEO` 文档）随主线 2「AI 文档整理页」一并落地。

## 7.16 已完成：个人 Wiki 两段式（改造现有自动向量化）

### 7.16.1 目标

个人库从「上传即自动解析向量化」改为两段式，确保进入个人向量库的内容是用户确认过的结构化知识，而不是原始文档全文。

### 7.16.2 流程

1. 上传：仅保存原始资源（可预览/下载/维护），不自动向量化。
2. 用户选择文档并点「生成 Wiki」：AI 提取整理为结构化内容（摘要/要点 + 标题层级，可编辑）。
3. 用户审核/编辑草稿。
4. 确认：以结构化内容落 `knowledge_doc` 并向量化上架（个人库）。

- 检索只查「已确认」记录；学习分析「已向量化文档」统计口径同步调整。

### 7.16.3 实现要点

- 草稿/已确认状态优先复用现有 `status` / `vectorStatus`，不新增完整 `wiki_page` 表（避免 DDL 膨胀）。
- 上传队列改为「保存 + 建草稿」，不触发向量化；确认动作单独入队。
- 与官方库「AI 文档整理」（7.17）共用一个「AI 结构化整理」能力组件。

### 7.16.4 实现记录（2026-08-22，代码交付待联调）

- **三层系统目录**：`resource_directory` 新增 `dir_type`（raw/wiki/attachments 为系统目录，NULL=普通目录）；新账号初始化即建三层，存量账号在 `getStorageInfo` 时幂等补齐（`StudentKnowledgeBaseServiceImpl.ensureSystemDirectories`）；系统目录禁止重命名/删除（Controller 拦截）。
- **上传归类与白名单**：未选目录时自动归类——md/txt 文档→raw，图片/视频/其他文档→attachments（`resolveDefaultDirectoryId`）；选中 raw 目录上传非 md/txt 时拒绝（`validateDirectoryState`，前端同步提示）；上传不再自动创建 `knowledge_doc`（删除 `createStudentKnowledgeDoc`）。
- **生成 Wiki 链路**：`StudentWikiGenerateComponent`（web 层，ChatClient 封装，>6000 字符分段整理后拼接，输出结构化 Markdown）；`StudentWikiServiceImpl.generateDraft` 提取源文档文本（复用 `ResourceKnowledgeParser`）→ AI 整理 → 落 `knowledge_doc` 草稿（`vectorStatus=0`，`source_resource_id` 指向资源，已存在则覆盖并清理旧向量）。
- **编辑 / 确认**：`updateDraft` 任意状态可编辑，编辑后回草稿态并清理旧向量；`confirm` 置 `vectorStatus=1` 入队；`StudentKnowledgeImportQueueListener` 消费时若 content 非空直接分块向量化（不再用源文件解析结果覆盖），完成后置 2。
- **检索与统计口径**：`RagSearchComponent.keywordSearch` 个人库强制 `vectorStatus=2`（草稿不进检索）；`LearningAnalysisBiz.studentDocList` 同步只统计已确认页。
- **级联清理**：删除学生资源时 `StudentWikiService.cleanupByResource` 级联删除知识页并清理 ES chunks。
- **学习档案**：新建 `user_wiki_profile` 表（学习目标 / 关键问题 / 兴趣主题 / 术语叫法），`GET|PUT /api/studentWiki/profile`；前端「我的学习档案」表单（独立 Modal）。
- **接口**：`POST /api/studentWiki/generate`、`PUT /draft`、`POST /confirm`、`GET /getInfo`、`GET /list`、`DELETE /del`、`GET|PUT /profile`（全部校验 owner）。
- **前端**：资源中心目录树展示系统目录中文名（原始资料/知识页/附件）并隐藏其管理按钮；「知识页」目录切换为知识页列表视图（WikiListPanel：编辑/确认入库/删除）；原始资料文档行内「生成 Wiki」按钮 → 生成后打开编辑弹窗（保存草稿 / 保存并确认入库）。
- **迁移脚本**：`nexora-java/sql/20260822_student_wiki.sql`（ALTER 加 `dir_type` + 新建 `user_wiki_profile`；不修改任何既有 DDL）。`docs/sql/20260822_student_wiki.sql` 与 `20260822_v2_email_account.sql` 两处副本已于 2026-08-22 删除——增量全部并入 `docs/sql/nexora_base.sql`（开发者库导出全量），团队迁移只跑 `migrate.bat` 即可。
- **遗留**：附件粘贴图片 + Obsidian `![[...]]` 引用渲染、word/pdf 归 raw 的扩展放下一迭代；官方库 AI 文档整理页（7.17）待主线 2。

## 7.17 已完成：官方库向量化前 AI 文档整理

### 7.17.1 目标

官方知识库入库前先由 AI 把提取文本整理成结构化 Markdown（一级/二级标题层级、去掉前言与噪声、只保留知识点内容），提升检索命中与回答质量。

### 7.17.2 流程

```text
选择资源 → 提取文本（复用 ResourceKnowledgeParser）→ AI 整理为结构化 MD
    → 管理员预览确认 → 分块向量化 → 更新 knowledge_doc / ES
```

- 超长文档分段整理，逐段拼接，避免截断丢内容。
- 原始文本保留在资源中心可对比；`knowledge_doc.content` 存整理后的结构化 MD。
- 失败重试走现有 Redis 异步队列；管理员可手动修正后再入库。

### 7.17.3 与个人 Wiki 的关系

- 共用同一套「AI 结构化整理」组件（prompt + 调用 + 重试）：官方库由管理员确认，个人库由用户确认。

### 7.17.4 实现记录（2026-08-22，代码交付待联调）

- **`AiStructureComponent`（common 层）**：AI 结构化整理公共能力——原始文本分段（单段 ≤6000 字符，超长按换行切分多段逐段整理后拼接）→ 输出标题层级 + 摘要 + 要点 Markdown；使用当前端默认对话模型（不绑定具体模型配置）；学生个人 Wiki（原 `StudentWikiGenerateComponent` 已删除，`StudentWikiServiceImpl` 直接注入公共组件）与管理端官方库整理共用同一条整理链路。
- **管理端接口**：`POST /api/knowledgeBase/aiOrganize?resourceId=`（仅官方资源 ownerId 为空）：提取文本（复用 `ResourceKnowledgeParser`）→ 整理 → 返回 `KnowledgeAIDocVO{resourceName, stage, originalText(≤60000 字符对照展示), organizedMd}`。
- **管理端页面**：「知识库管理 → AI 文档整理」（`/knowledge/arrange`，菜单 `knowledge:arrange`）：左侧资源表格（名称/类型/学段/大小，单选）→ 右侧工作区「AI 整理」→ 原始提取文本 / 整理稿左右分栏（整理稿可编辑、可重新整理）→ 「确认入库」弹层（标题/学段/知识点/难度，知识点来自知识目录树）→ 复用 `resourceImport`（content 模式，sourceType=2）异步向量化。
- 遗留：官方图片/视频轻量向量化（`dataType=IMAGE/VIDEO` 文档）挂本主线下一迭代；docx 解析按钮（7.18 的习题页已有同类能力，此处资源解析已覆盖 docx）。

## 7.18 已完成：习题批量导入页重构

### 7.18.1 目标

放弃 PDF 文本/公式自动解析，改为管理员将 PDF 转 Word 后导入系统成 MD；页面为「左侧 MD 编写 + 右侧 KaTeX 预览 + docx 解析按钮」。

### 7.18.2 交互

- 左侧 MD 编辑器（复用 `@uiw/react-md-editor` + KaTeX），右侧实时预览。
- 「docx 解析按钮」：上传 `.docx` → POI 提取段落/表格 → 填入左栏作为 MD 初稿，公式由管理员用 LaTeX 手写。
- 不再提供 PDF 自动解析/页图模式（7.7.4 已废弃）；PDF 仅作人工转 Word 的来源提示。
- AI 出题模式保留（7.7.3）。

### 7.18.3 产出

- 编辑后的 MD 可：直接结构化解析入库（复用 `parseMd / batchAdd`）、保存资源中心（DOCUMENT 资源）。

### 7.18.4 实现记录（2026-08-22，代码交付待联调）

- 习题管理 Tab 由「PDF 导入」更名「批量导入」，页面重构：说明 Alert（PDF 转 Word 提示）+ 工具条（选择 Word / 解析 Word 为 MD 初稿 / 下载 MD / 解析并入库）+ 左编辑（`MDEditor preview="edit"`、remark-math + rehype-katex）右预览（`MDEditor.Markdown` 只读渲染）。
- 后端 `POST /api/questionImport/parseDocx`（multipart）：POI `XWPFDocument` 提取段落文本（非空行）与表格（`|` 分隔单元格），返回纯文本初稿。
- PDF 旧后端（parsePdf / pageImage）保留但前端入口移除，不推荐使用。

### 7.18.5 验收

- 进入「习题管理 → 批量导入」：上传 .docx →「解析 Word 为 MD 初稿」→ 左栏出现提取文本；按题目 MD 规范整理（`### 题目 N` 块 + 年级/难度/题型/分值/知识点/题干/选项/答案/解析，公式 LaTeX）→ 右栏预览公式正确渲染；「解析并入库」→ 题目列表草稿可见 → 审核上架。

## 7.19 待开发：MCP 教学工具服务（P4）

### 7.19.1 定位

MCP 是「教学域能力出口」，不是业务主链路。AI 助教问答、RAG、个人 Wiki 继续走 `nexora-web` 内部组件；MCP 只把少量白名单能力暴露给外部 AI 平台 / 第三方 agent / 演示解耦使用。`nexora-web` 的 MCP 客户端保持默认关闭，开启后作为工具能力，关闭不影响对话。

### 7.19.2 第一版工具清单（白名单，5 个）

- `queryKnowledgePoint(stage, keyword)`：查知识点（ID / 学段 / 学科 / 描述）。
- `queryCourse(stage, keyword)` / `queryLesson(courseId, lessonId)`：查课程与课时资源。
- `recommendResource(stage, knowledgePointId, type)`：返回资源卡片（resourceId / type / title / sourceUrl）。
- `saveLearningRecord(userId, targetId, type, detail)`：写 `student_learning_record`，供学习分析与知识层落地。
- `queryMastery(userId, stage)`：查知识层 / 掌握度概览。

暂缓：`generateQuiz / autoGrade / planNextStep / createStudyPath`。对话内出题（L4）与学习路径主链路稳定后再补，避免两头维护。

### 7.19.3 工程结构

```text
nexora-mcp/
  src/main/java/com/nexora/
    NexoraMcpServerApplication.java      # 已有
    config/ServerRegisterConfig.java     # ToolCallbackProvider Bean
    service/TeachingToolService.java     # 工具即 Service 方法
```

- 每个工具 = `@Tool` 方法，参数 `@ToolParam`，返回 `String`，方法内 try-catch，不抛未捕获异常。
- 工具只调 `nexora-common` 的 Service / 组件，不直接操作 Mapper / SQL / 文件。
- `ServerRegisterConfig` 用 `MethodToolCallbackProvider.builder().toolObjects(teachingToolService).build()` 注册。
- WebFlux + Streamable HTTP + `/mcp`（8084）：pom / yml 已就位，不改协议。

### 7.19.4 调用与安全

- MCP Server 仅内网 / 本机调用，不直接暴露公网。
- 用户 ID 由 web 端登录上下文替换后传入，MCP 服务不信任工具入参中的 userId；写操作必须校验。
- 生成类工具不落「已确认」数据；不提供任意 SQL / 文件 / 系统操作工具。

### 7.19.5 实施顺序

1. 三个查询工具 + `ServerRegisterConfig`，启动 8084 自测，`tools/list` 能列出工具。
2. 补 `saveLearningRecord / queryMastery` 两个读写工具，接学习分析数据。
3. 接 web 端客户端：保持 `NEXORA_MCP_CLIENT_ENABLED=false` 默认关闭，验证开启后可用工具、关闭后降级正常。

### 7.19.6 验收

- 8084 启动无冲突，`tools/list` 列出 5 个教学工具。
- 开启 web 端客户端后，对话可发起工具调用；关闭后对话正常降级。
- 工具清单全部为教学域白名单，无任意 SQL / 文件 / 系统操作。

### 7.19.7 实现记录（2026-08-22，代码交付待联调）

- `TeachingToolService`（mcp 模块 `com.nexora.service`）：`queryKnowledgePoint`（学段+名称模糊）、`queryCourse`（上架+学段+名称模糊）、`queryLesson`（courseId 列表或 lessonId 详情）、`recommendResource`（官方资源 ownerId 为空 + 学段/知识点/类型，取前 10 条卡片文本）、`queryMastery`（复用 `LearningAnalysisMapper.selectMasteryList` 带知识点名称聚合）、`saveLearningRecord`（校验学生存在、行为类型白名单 VIEW/COMPLETE/PRACTICE/ANIMATION/PARSE/AI_CHAT、目标按资源/课程/课时顺序识别）——全部返回 String，内部 try-catch 不抛未捕获异常。
- `ServerRegisterConfig`：`MethodToolCallbackProvider.builder().toolObjects(teachingToolService).build()` 注册为 Bean。
- 补齐 `student_learning_record` common 层全套（PO/Query/Mapper/XML/Service/Impl，表已存在无 DDL 变更），供 MCP 与后续学习分析使用。
- 删除 mcp 模块空 `InitRun.java`。
- web 端 MCP 客户端维持 `NEXORA_MCP_CLIENT_ENABLED=false` 默认关闭（7.19 定位：教学域能力出口，不参与主链路）。

## 7.20 已完成：动画讲解产物链路（对话内生成 SVG 分步讲解）

### 7.20.1 目标

ANIMATION 意图从「只回文本脚本」升级为真实产物：AI 生成分步 SVG 讲解脚本，对话内以可播放卡片呈现，并在动画独立页统一回看，形成「对话问答 + 动画讲解」两种真实多模态产物（对齐 4.6 五选三的动画讲解）。

### 7.20.2 产物结构

脚本 JSON（存 `ai_generation_record.content` 与 `agent_message.bizData`）：

```json
{
  "title": "冒泡排序",
  "steps": [
    { "title": "第一步：比较相邻元素", "explain": "讲解文字", "svg": "<svg ...>...</svg>" }
  ]
}
```

- 4-8 步、逐步递进；每步标题 + 讲解 + 自包含 SVG 画面（640x400 基础图形）。

### 7.20.3 实现记录（2026-08-22，代码交付待联调）

- 后端 `AnimationScriptComponent`（web 层）：LLM 生成脚本 JSON（学段适配提示词）→ 解析校验（steps 1..12）→ 逐帧 SVG 白名单清洗：
  - 提取 `<svg>...</svg>` 块；删除 script/style/foreignObject/iframe/object/embed/link/meta 标签与事件属性/href/javascript: 引用；
  - 仅保留基础图形标签白名单（svg/g/rect/circle/ellipse/line/polyline/polygon/path/text/tspan/defs/gradient/stop）；
  - 单帧超 6000 字符丢弃该帧，保证 WS 消息体积可控。
- `AgentChatComponent`：intent=ANIMATION 时走 `handleAnimationAnswer`——生成脚本 → 落 `ai_generation_record`（type=ANIMATION、source=0 学生生成）+ **同步落个人知识库 `resource_info`（resourceType=ANIMATION、附件目录、ext_json=脚本）** → 更新 `agent_message.bizType=ANIMATION/bizData=脚本` → WS 推送 `done + ANIMATION` 卡片；生成失败自动降级为文字讲解（intent 回退 CHAT 继续流式）。
- 前端：对话内动画卡片改为「小卡片（标题+步数+『查看动画讲解』按钮）」，点击跳转独立页 `/animation`（列表）与 `/animation/:resourceId`（大屏全尺寸播放页，`AnimationPlay`，复用 `SvgStepPlayer`）；历史消息重放保留卡片；动画列表页数据源改为个人知识库 ANIMATION 资源（`/api/animation/myList`，删除同步删资源）。
- 变更说明：2026-08-22 起动画产物=个人知识库资源，资源中心「附件」目录可见可删；`ai_generation_record` 保留作过程记录。
- **学段限制（2026-08-22 补充）**：动画讲解现仅面向初中/高中学段——前端 AI 助教对小学阶段（PRIMARY_LOW/PRIMARY_HIGH）隐藏全部动画入口（欢迎建议词/动作按钮/模式切换），后端 `IntentAnalyzerComponent.analyze(userMessage, stage)` 对小学阶段硬拦截：提示词显式禁止 ANIMATION + 结果 `ANIMATION` 一律降级 `CHAT` 文字讲解（防止误用绘本生图链路/无页面可看）。

### 7.20.4 验收

- AI 助教输入「生成一个冒泡排序的动画讲解」→ 收到「已为你生成动画讲解《...》」+ 可播放分步卡片（步骤可切换、SVG 画面可见、讲解文字正确）；刷新后历史消息里卡片仍在。
- 动画独立页展示该动画，可全屏播放、删除；删除后独立页与对话历史卡片（独立页删除不影响 agent_message 历史，如需要后续做级联）保持一致。
- 生成失败（模型输出无法解析）时对话降级为文字讲解，不报错。

## 7.21 已完成：对话内出题卡片（L4）

### 7.21.1 目标

QUIZ 意图从「只回文本题目」升级为真实答题产物：AI 生成选择测验，对话内以答题卡片呈现，作答后即时判分反馈，形成「讲解 → 练习 → 即时反馈」的对话内完整学习闭环（对齐 4.6 五选三中的游戏化练习的最小形态；落 `practice_record / knowledge_mastery` 部分等「最小答题判分闭环」团队确认，见主线 10）。

### 7.21.2 产物结构

测验 JSON（存 `agent_message.bizData`）：

```json
{
  "title": "冒泡排序小测验",
  "questions": [
    { "type": "SINGLE", "question": "题干", "options": ["A. ...", "B. ...", "C. ...", "D. ..."], "answer": 0, "analysis": "解析" }
  ]
}
```

### 7.21.3 实现记录（2026-08-22，代码交付待联调）

- 后端 `QuizGenerateComponent`（web 层）：LLM 生成测验 JSON（学段适配）→ 解析校验（3-5 题、每题选项 ≥2、answer 下标在选项范围内、含解析，超 6 题截断），不合法抛异常。
- `AgentChatComponent`：intent=QUIZ 时走 `handleQuizAnswer`——生成测验 → 更新 `agent_message.bizType=QUIZ/bizData=测验JSON` → WS 推送 `done + QUIZ` 卡片；生成失败降级为文字出题（intent 回退 CHAT 继续流式）。`ANIMATION/QUIZ` 降级逻辑抽为 `degradeToChat`。
- 前端 `components/multimodal/QuizCard.tsx`：逐题单选 → 「提交答案」即时判分（每题为绿色/红色边框 + ✓/✗ + 解析，底部得分与鼓励文案）→「再练一次」重置；选项文本剥离去掉 "A. " 前缀；AI 助教页收到 `bizType=QUIZ` 渲染答题卡片，历史消息重放保留卡片。
- 安全：题目内容经 React 默认转义渲染，无注入面。
- 不落 `practice_record / knowledge_mastery`：等团队确认「最小答题判分闭环」后接入（主线 10）。

### 7.21.4 验收

- AI 助教输入「帮我做一道练习」/「出几道题考考我」→ 收到「我出好了《...》」+ 答题卡片：每题可作答、提交立即判分显示对错与解析、得分统计、可重练；刷新历史后卡片仍在。
- 生成失败（模型输出无法解析）时对话降级为文字出题，不报错。

## 7.22 已完成：绘本生成（小学阶段，异步任务编排）

### 7.22.1 目标

面向小学阶段（小低 + 小高）：输入主题 → **主题净化（版权预设）** → AI 编故事（4-8 页分页文案）→ **异步并发生图** → 图文翻页绘本；产物存 `resource_info.ext_json`（PICTURE_BOOK 类型），绘本页统一回看（二期 TTS 配音已取消，一期即完整形态）。

### 7.22.2 产物结构

`resource_info`（owner 隔离，source=1 AI 生成，resourceType=PICTURE_BOOK，归入「附件」目录）的 `ext_json`：

```json
{
  "type": "PICTURE_BOOK",
  "pages": [
    { "text": "第1页文案", "imageFile": "resource/files/student/{邮箱目录}/picture-book/20260822/xxx.png" }
  ]
}
```

### 7.22.3 实现记录（2026-08-22，代码交付待联调）

- **异步任务编排（替代同步长阻塞）**：
  - `POST /api/pictureBook/generate` 立即返回 `taskId`（不再阻塞 HTTP）；`GET /api/pictureBook/task?taskId=` 查询状态；
  - `PictureBookTaskService(Impl)`：任务体 JSON 存 Redis（`picturebook:task:`，TTL 2h），`leftPush` 入队 `picturebook:task:queue`；
  - `PictureBookTaskConsumer`（@Scheduled 消费）：状态机 `PENDING → STORY_GENERATING → STORY_DONE → IMAGE_GENERATING → COMPLETED/FAILED`，每页完成回写 `current/total` 与 `imageFile`（断点：重启后任务丢失，前端提示重试，MySQL 断点续跑为后续项）；
  - **图片 3 路并发**（固定线程池），单页失败不废弃整本；页面全失败把原因写入 `ext_json.imageError`。
- **版权预设（内容安全）**：
  - `sanitizeTopic`：任务执行第一步调 LLM 把主题解构为原创表述（米老鼠→戴小红帽爱冒险的小老鼠等，30 字内，失败回落原主题）；
  - 故事提示词：强制原创角色，禁止 IP 角色与标志性情节（毒苹果/水晶鞋/魔镜），命中自动改写；
  - 生图提示词：严禁任何知名动画/童话 IP 角色与标志性元素。
- **文生图供应商**：默认豆包（`project.ai.image.provider=ark`）：
  - ARK OpenAI 兼容 `https://ark.cn-beijing.volces.com/api/v3/images/generations`：`model=doubao-seedream-5-0-pro-260628`、`response_format=url`、`output_format=png`、`size=1.5K`、`optimize_prompt_options.mode=fast`、`NEXORA_ARK_API_KEY`（用户级环境变量，禁止入库）；
  - 单次请求 90s 超时（豆包生成 20-60s，dashscope 同步保持 30s）、429 与网络异常自动重试 3 次；
  - 错误归类：Key 无效（含掩码提示）/限流/额度/内容安全（PolicyViolation）/接口原文；
  - dashscope（qwen-image-3.0/3.0-pro、专属地域域名）保留可切回。
- 落库与接口：`PictureBookServiceImpl.saveBook` 统一落库（附件系统目录）；`/myList`、`/getInfo`、`/del`（级联删图）、`/image/{resourceId}?page=N`（页图公开直连）。
- 前端 `picture-book/index.tsx`：
  - 提交任务 → 2s 轮询，按钮实时显示「AI 正在编写故事.../正在绘制插图 x/N 页...」；完成自动刷新列表并打开绘本；失败展示 `imageError` 原因；
  - **切页不丢任务**：任务状态持久化 sessionStorage（`pb:task`），轮询仅页面存活时跑；切回自动恢复进度或直接打开成品/提示失败；
  - 学段开放：小学阶段（小低+小高）可进入；初中/高中显示「绘本面向小学阶段」占位。
- 图片存储：`resource/files/student/{邮箱小写,@→_at_}/picture-book/{yyyyMMdd}/`。

### 7.22.4 验收

- 小学阶段学生登录 → 绘本页输入主题（如「机器人朋友」）→「生成绘本」→ 按钮出现进度 → 1-3 分钟完成自动打开（插图 + 文案翻页）→ 切到其他页再切回：进度恢复 / 完成后自动打开。
- 输入「米老鼠的故事」→ 自动改编为原创小鼠主题，全页出图且不触发平台版权拦截。
- 无图页（失败页）显示「本页暂无插图」+ 失败原因（如额度/内容安全），故事仍可读。
- 绘本出现在资源中心「附件」目录，类型「绘本」，可删（删除级联图文件）。
- 依赖：`NEXORA_ARK_API_KEY`（或 dashscope 回退）有效；豆包模型已开通且账户有额度；网络可达方舟（90s 超时已放宽）。

## 7.23 已完成：编程环境一期（学生端）

### 7.23.1 目标

面向初中 / 高中（小高也可用）的浏览器内 Python 编程环境：预置代码框架、可运行调试、输出即时可见；「保存学习」把代码沉淀进个人知识库。

### 7.23.2 实现记录（2026-08-22，代码交付待联调，纯前端改动）

- 页面 `nexora-front-web/src/views/coding/index.tsx` 由占位页升级：
  - **编辑器**：`@monaco-editor/react`（vite 打包，loader 默认 CDN 加载 monaco，首次加载需网络）；
  - **运行**：动态 `import('pyodide')` + `loadPyodide({ indexURL: CDN v0.26.4 full })`（浏览器内 Python 3.12，首次加载约 10-30 秒，缓存后复用）；`setStdout/setStderr` 批处理采集输出，`runPythonAsync` 执行，支持 input()（内置引导）；
  - **预置框架**：按 `userInfo.stage` 切换——小高：循环打印图形 / 猜数字小游戏；初中：列表与统计 / turtle 海龟画图；高中：冒泡排序（过程展示）/ 递归斐波那契；未登录默认初中；
  - **交互**：运行（loading 与耗时显示）/ 重置 / 清空输出；输出面板深色终端风格；
  - **保存学习**：代码以 Markdown 包装（```python 代码块）→ 构造 .md 文件 → 走现有分片上传接口保存到个人库 raw（自动归类目录）→ 提示可在资源中心对其「生成知识页」，与 P0 两段式打通。
- 后端零改动；`pyodide`、`@monaco-editor/react`、`monaco-editor` 依赖已在 package.json 且已安装。

### 7.23.3 验收

- 学生端（初中/高中）→ 编程环境：切换预置框架、编辑代码、点击运行看到输出与耗时；改错代码运行能看到报错信息；小高账号看到对应示例。
- 「保存学习」→ 个人库「原始资料」出现该 .md → 可对其生成知识页并确认入库。
- 离线环境（无 CDN）运行时编辑器/Pyodide 加载会失败并提示网络错误，属已知限制（评审现场需联网）。

## 7.24 已完成：答疑后动作卡片 + 对话同步知识页（L2/L3）

### 7.24.1 目标

AI 讲解完成后，在回复下方出现「同步知识页 / 出题练习 / 动画讲解」动作卡片，把问答沉淀为个人知识页草稿（两段式确认后入向量库），形成「讲 → 练 → 沉淀」闭环；出题/动画动作复用现有意图链路。

### 7.24.2 实现记录（2026-08-22，代码交付待联调）

- 后端 `POST /api/studentWiki/syncFromMessage?messageId=`：校验消息属于当前用户且已完成（status=1、回答非空）→ 以「用户问题 + AI 讲解」生成知识页草稿（标题=问题前 30 字，`sourceType=0`，`sourceUrl="agent-message:"+messageId` 作消息级去重标识，重复同步覆盖草稿并清理旧向量，`vectorStatus=0` 待确认）。
- `KnowledgeDocQuery` 新增 `sourceUrl` 精确过滤（Mapper XML 同步）。
- 前端 AI 助教页：讲解类回复（无动画/测验卡片、非 pending）下方渲染动作按钮组：
  - **同步知识页**：调 `syncFromMessage` → 提示「已生成知识页草稿，可在『知识页』目录查看并确认入库」；
  - **出题练习**：以「针对刚才讲解的内容出几道练习题考考我」继续对话（走 QUIZ 意图）；
  - **动画讲解**：以「把刚才讲解的内容生成一个动画讲解」继续对话（走 ANIMATION 意图）。

### 7.24.3 验收

- 讲解完成后回复下方出现三个动作按钮；点「同步知识页」→ 资源中心「知识页」目录出现草稿（标题=问题）→ 编辑/确认入库后可被检索。
- 点「出题练习」「动画讲解」→ 正常生成答题卡片 / 动画卡片。
- 同一消息重复点击同步 → 草稿覆盖不产生重复记录。

## 8. 本地运行

### github开发者：AAAQ-ljt
- 学生端前端：`nexora-front-web`，端口 `3000`，代理 `/api` → `6060`，`/ws` → `6062`。
- 管理端前端：`nexora-front-admin`，端口 `3001`，代理 `/api` → `6061`。
- 学生端后端：`nexora-web`，接口 `6060`，WebSocket `6062`。
- 管理端后端：`nexora-admin`，接口 `6061`。
- MCP：`nexora-mcp`，端口 `8084`。
- MySQL `3306`、Redis `6379`、ES `9200`。
- Maven：统一使用 `E:\dev\maven\apache-maven-3.9.10\bin\mvn.cmd`（Java 21）。
- **数据库一键迁移（团队用）**：`docs\sql\migrate.bat`（已随 SQL 迁至 docs 下）——运行即**清除旧库重建**并导入 `docs\sql\nexora_base.sql`（从开发者库导出的 35 表最新结构，业务数据清除，唯一基线；旧 schema 与增量脚本已删除，不再区分 full/upgrade）。
- **文生图 Key（绘本）**：`NEXORA_ARK_API_KEY`（火山方舟，用户级 `setx` 或 IDEA 运行配置，禁止入库）；完整变量清单与作用域说明见 `docs/联调验收.md` 1.2 与 `docs/联调验收.md`（环境变量一节）。

### github开发者：LoveCodeWalf
- 学生端前端：`nexora-front-web`，端口 `3000`，代理 `/api` → `6060`，`/ws` → `6062`。
- 管理端前端：`nexora-front-admin`，端口 `3001`，代理 `/api` → `6061`。
- 学生端后端：`nexora-web`，接口 `6060`，WebSocket `6062`。
- 管理端后端：`nexora-admin`，接口 `6061`。
- MCP：`nexora-mcp`，端口 `8084`。
- MySQL `3306`、Redis `6379`、ES `9202`。
- 后端工具链：JDK 21 在 `E:\develop_software\java\jdk\jdk-21.0.11`；当前 `JAVA_HOME` 默认指向 Java 8（`E:\develop_software\java\jdk\java1.8_jdk`），跑 Maven 前必须先设置 `JAVA_HOME=E:\develop_software\java\jdk\jdk-21.0.11`。
- Maven：`E:\develop_software\java\maven\apache-maven-3.9.11\bin\mvn.cmd`，本地仓库为 `E:\develop_software\java\maven\nexora_repo`。
- Maven 注意：命令行 Maven 默认读取全局 `settings.xml`，其中定义的本地仓库是 `E:\java\spring_study_repository`（当前不存在），所以直接 `mvn` 会失败；本机辅助文件 `E:\github_project\k12\nexora\.maven-settings.xml` 用 `-s` 显式覆盖为你实际使用的 `E:\develop_software\java\maven\nexora_repo`，后端编译必须带该参数，并设置 `MAVEN_OPTS=-Dfile.encoding=UTF-8` 避免中文编码报错。该辅助文件不随功能提交。
- 前端工具链：Node 在 `E:\develop_software\node\nodejs22.23.1`；PowerShell 中请使用 `npm.cmd run build`，直接 `npm` 会命中未签名的 `npm.ps1` 被执行策略拦截。
- 常用后端编译命令：
  ```powershell
  cd E:\github_project\k12\nexora\nexora-java
  $env:JAVA_HOME="E:\develop_software\java\jdk\jdk-21.0.11"
  $env:MAVEN_OPTS="-Dfile.encoding=UTF-8"
  E:\develop_software\java\maven\apache-maven-3.9.11\bin\mvn.cmd -s E:\github_project\k12\nexora\.maven-settings.xml -pl nexora-admin -am compile -DskipTests
  ```
- 常用前端构建命令：
  ```powershell
  cd E:\github_project\k12\nexora\nexora-front\nexora-front-admin
  npm.cmd run build
  ```

## 9. AI 交接提醒

- 历史排期文档已删除，不再作为执行依据；一切开发判断以本文件为准。
- 一切开发判断以本文件为准；新功能按功能块逐个确认后再开发。
- `参考项目/` 已忽略，只读，不修改、不提交。
- 管理端资源接口在 `nexora-admin`（公共资源 `owner_id IS NULL`）；学生资源中心在 `nexora-web`（`/studentResource/*`，仅操作本人资源）。
- 学生端 AI 链路在 `nexora-web`。
- AI 对话必须保持 Netty WebSocket 流式 / 异步，禁止同步阻塞。
- 管理端/学生端统一 React 19 + antd v6，新页面优先复用现有组件。
- 学生端课程教材的媒体接口（`video/image/file/download`）按 HLS/图片/文档直连需要公开；登录校验覆盖列表与详情接口。
- 学生端后端 MCP 客户端默认关闭，需开启时设置 `NEXORA_MCP_CLIENT_ENABLED=true` 并先启动 `nexora-mcp`。
- 启动联调前确认 6060 / 6061 / 6062 / 8084 端口未被旧进程占用。
- 编译/构建/打包/启停协作（2026-08-22 起，见根目录 `AGENTS.md`「编译/启动/验证协作流程」）：Codex 负责 `mvn` / `npm` 的 compile / install / build 与服务启停、端口验证；用户在 Codex 开始前停掉旧端口，Codex 验证完毕后主动关停服务，最终验收启动由用户执行。
- 向量化入库前必须先 AI 整理 + 确认：官方库由管理员确认，个人库由用户确认后写入知识库。
- 当前对话模型 DeepSeek 仅用于开发测试；目标为多模态、教育能力强的模型（如 Gemini），切换时只改配置，不动业务封装。
